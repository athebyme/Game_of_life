#include "../Headers/Grid.h"

struct grid{
    int width;
    int height;
    int ** grid;
};

struct new_grid{
    int width;
    int height;
    int ** grid;
};

int nextGeneration(){
    return 0;
}